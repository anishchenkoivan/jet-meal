apiVersion: v1
kind: Service
metadata:
  name: businesses-service
  namespace: jet-meal
spec:
  selector:
    app: businesses-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
