apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: jet-meal
spec:
  selector:
    app: billing-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
