apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: jet-meal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: billing-postgres-db
  namespace: jet-meal
spec:
  serviceName: billing-postgres-db
  replicas: 1
  selector:
    matchLabels:
      app: billing-postgres-db
  template:
    metadata:
      labels:
        app: billing-postgres-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: billing_db
            - name: POSTGRES_USER
              value: billing_service
            - name: POSTGRES_PASSWORD
              value: TODO_secrets
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-pvc

---

apiVersion: v1
kind: Service
metadata:
  name: billing-postgres-db
  namespace: jet-meal
spec:
  selector:
    app: billing-postgres-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
