apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: jet-meal
  labels:
    app: billing-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      containers:
        - name: billing-service
          image: billing-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: BILLING_POSTGRES_DSN
              valueFrom:
                configMapKeyRef:
                  name: billing-config
                  key: postgres.dsn
            - name: BILLING_PORT
              valueFrom:
                configMapKeyRef:
                  name: billing-config
                  key: billing.port
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
