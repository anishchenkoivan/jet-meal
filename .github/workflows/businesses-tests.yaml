name: Run tests for businesses service

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'services/businesses/**'
      - 'libraries/c/**'
      - 'scripts/openapi-c-gen/**'
  push:
    branches: [ main ]
    paths:
      - 'services/businesses/**'
      - 'libraries/c/**'
      - 'scripts/openapi-c-gen/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Run tests
        run: make -C services/businesses tests
