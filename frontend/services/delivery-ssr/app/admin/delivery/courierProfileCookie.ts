/** HttpOnly: наличие оформленного профиля курьера (демо). Имя вынесено из `actions.ts`, т.к. в `"use server"` нельзя экспортировать не-функции. */
export const COURIER_PROFILE_COOKIE = "jet-meal-courier-profile";
